//const { scooterService } = require('../scooter_service');

describe('ScooterHandler', () => {

    beforeEach(() => {
      
    });
  
    describe('test', () => {
      it('test', () => {
        expect(1).toEqual(1);
      });
  
    });
  });