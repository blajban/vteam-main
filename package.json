{
  "devDependencies": {
    "jest": "^29.3.1"
  },
  "scripts": {
    "test": "jest --verbose --coverage"
  },
  "jest": {
    "testPathIgnorePatterns" : [
      "<rootDir>/services/app/.",
      "<rootDir>/services/web/."
    ]
  }
}
